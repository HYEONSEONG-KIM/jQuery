<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0/EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
	
<sqlMap namespace="member">
	<select id="selectAll" resultClass="memberVo">
		select * from member
	</select>

	<select id="selectOne" resultClass="memberVo" parameterClass="string">
		select * from member where mem_id = #id#
	</select>
	
	<select id="login" resultClass="memberVo" parameterClass="map">
		select * from member where mem_id = #mem_id# and mem_pass = #mem_pass#
	</select>
	
	<select id="idCheck" resultClass="string" parameterClass="string">
		select mem_id from member where mem_id = #id#
	</select>
	
	<select id ="searchZip" resultClass="zipVo" parameterClass="string">
		select * from ziptb where dong like #dong#||'%'
	</select>
	
	<insert id="insertMember" parameterClass="memberVo">
	insert into member(MEM_ID,MEM_PASS,MEM_NAME,MEM_BIR,
	MEM_ZIP,MEM_ADD1,MEM_ADD2,MEM_HP,MEM_MAIL) 	values(#mem_id#,#mem_pass#,#mem_name#,#mem_bir#,#mem_zip#,#mem_add1#,#mem_add2#,
	#mem_hp#,#mem_mail#)
	</insert>
</sqlMap>